#include <stdio.h>
#include <math.h>


int main (){
	
	int a;
	unsigned int b;
	char c;
	unsigned char d;
	short int e;

	printf("%i %i %i %i %i\n", sizeof(a), sizeof(b), sizeof(c), sizeof(d), sizeof(e));
	return 0;
}
